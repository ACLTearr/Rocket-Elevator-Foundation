require 'rails_helper'

describe User do
    context 'testing' do
        it 'tests' do
            # user = User.new(email: nil)
            # expect(user).to_not be_valid
        end
    end
end